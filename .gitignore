# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.env

# Per-deployment compose override (copied from the .example, holds local paths).
# Compose auto-merges it, so it is easy to edit and then commit by accident.
docker-compose.override.yml

# Runtime output
exports/
*.zip
*.log
*.patch

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage

# macOS
.DS_Store
sensors.json
